version: 2

build:
  os: ubuntu-24.04
  tools:
    python: '3.11'

mkdocs:
  configuration: examples/config/mkdocs.yml
  fail_on_warning: true

python:
  install:
    - requirements: examples/config/requirements.txt
